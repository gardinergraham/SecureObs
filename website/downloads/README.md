# SecureObs Android release

Place the signed production APK in this directory with this exact filename:

`SecureObs.apk`

Update `release.json` whenever a new APK is published. Keep the Android package name and production signing key unchanged so installations can be upgraded in place.
