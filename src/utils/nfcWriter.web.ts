export async function writeNfcTextPayload() {
  throw new Error(
    "NFC tag writing is available on NFC-enabled Android tablets. Save the staff member on Windows, then write their NFC tag from an Android tablet."
  );
}
